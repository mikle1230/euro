# DeepSeek API Key
# 获取地址: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-your-key-here

# 汇率转换 API Key（exchange-rate-api.com，免费版 Pair Conversion）
# 获取地址: https://www.exchangerate-api.com/ 注册后免费获取 Key
# 注意：这个 Key 只在前端调 /api/fx 时由服务端读取，值不会泄露到浏览器。
# 本地开发放在 .env.local（已被 gitignore，不入库）；**线上部署必须在托管平台的环境变量里配置同名变量**，
# 请勿把 Key 写进 .env.example / 仓库，否则会公开泄露。
EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key-here

# 可选：解析接口鉴权 Token（部署到公网时务必设置，防止 API 额度被刷）
# 前端在「行程设置」弹窗里填入同一个值，请求时带 Authorization: Bearer <token>
# PARSE_API_TOKEN=your-secret-token
